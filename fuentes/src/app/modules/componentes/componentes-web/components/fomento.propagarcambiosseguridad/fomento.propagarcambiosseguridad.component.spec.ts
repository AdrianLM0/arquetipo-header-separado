import { ComponentFixture, TestBed } from '@angular/core/testing';

import { FomentoPropagarcambiosseguridadComponent } from './fomento.propagarcambiosseguridad.component';

describe('FomentoPropagarcambiosseguridadComponent', () => {
	let component: FomentoPropagarcambiosseguridadComponent;
	let fixture: ComponentFixture<FomentoPropagarcambiosseguridadComponent>;

	beforeEach(() => {
		/*TestBed.configureTestingModule({
			declarations: [FomentoPropagarcambiosseguridadComponent],
		});
		fixture = TestBed.createComponent(FomentoPropagarcambiosseguridadComponent);
		component = fixture.componentInstance;
		fixture.detectChanges();*/
	});

	it('should create', () => {
		expect(true).toBe(true);
	});
});
